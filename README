== Arel Joins testing

This is a fairly simple application to test out Arel and its ability to join relatively involved relations.

Run `rake spec` to see five examples, of which currently two are failing.